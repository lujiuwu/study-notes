
### 2. 两数相加
**示例 1：**

![](https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2021/01/02/addtwonumber1.jpg)

**输入：** l1 = [2,4,3], l2 = [5,6,4]
**输出：** [7,0,8]
**解释：** 342 + 465 = 807.

#### 迭代

```js
var addTwoNumbers = function(l1, l2) {
  const head = null,tail = null,carry = 0
  while(l1||l2){
    // 获取同位节点值
    let val1 = l1.val?l1.val:0
    let val2 = l2.val?l2.val:0
    // 获取计数总和
    let sum = val1+val2+carry
    // 创建节点
    if(!head){
      // 考虑进位情况
      head = tail = new ListNode(sum%10)
    }else {
      tail.next = new ListNode(sum%10)
      tail = tail.next
    }
    // 更新carry值
    carry = Math.floor(sum/10)
    if(l1){l1 = l1.next}
    if(l2){l2 = l2.next}
  }
  // 考虑最后一个节点
  if(carry) {
    tail.next = new ListNode(carry)
  }
  return head


}
```

#### 递归

**创建新节点**
```js
// 递归解法

    // 边界情况

    if(l1 === null && l2 === null && carry === 0) return null

    let s = carry

    if(l1) {

        s+=l1.val

        l1 = l1.next

    }

  

    if(l2) {

        s+=l2.val

        l2 = l2.next

    }

  

    return new ListNode(s%10,addTwoNumbers(l1,l2,Math.floor(s/10)))
```

**原地修改**

### 19. 删除链表的倒数第N个节点

**my**
```js
var removeNthFromEnd = function(head, n) {

    let tail = head,len = 0

    while(tail){

        tail = tail.next

        len++

    }

    n = len-n+1

    let nextNode = head

    let step = 1

    // 删除头结点

    if(n===1) return head.next

    while(nextNode){

        if(step===n-1){

            nextNode.next = nextNode.next?nextNode.next.next:null

            console.log(nextNode,head)

            return head

        }

        nextNode = nextNode.next

        step++

    }

};
```

**指针？**

```js
var removeNthFromEnd = function(head, n) {

   const dummy = new ListNode(-1,head)

   let right = left = dummy

   while(n--){right = right.next}

   while(right.next){

    right = right.next

    left = left.next

   }

   left.next = left.next.next

   return dummy.next

};
```