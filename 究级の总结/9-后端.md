
### node.js

#### 常见模块
* path路径
* os操作系统
* file文件

#### buffer

* Buffer 是 Node.js 中用于直接操作二进制数据的核心模块，它提供了一种处理原始内存分配和操作的方式，特别适合处理 TCP 流、文件系统操作等 I/O 场景

**创建**

```js
const buf = Buffer.alloc(10) // 五字节的Buffer，默认填充0
```

**使用场景**
* 文件操作，图像处理


